# Utilities

## Directory structure
`ransac_benchmark` : Scripts and data to generate graphs in report <br>
`saved_map_for_sim` : Saved map of environment used in simulation<br>
`startup`: Dockerfile and bash scripts used to build and run the project